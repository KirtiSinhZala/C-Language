  # RULES & IMPORTANT NOTES

  structures are colllection of values of different data types.

 structure -> user defined Data Type.

 structure are stored in contiguous memory location

In function, when structure passed as parameter so it follow  CALL BY VALUE  approach