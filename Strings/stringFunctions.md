



 # Dengerous & Outdated  because it does not track MAX-SIZE of string
1. gets(str) -> input a string(even multi-word)

2. fgets(str, n, file) -> stops when n-1 chars input or new line is entered     (str->variable name, n->size, file->stdin)

3.  puts(str)  -> output a string and give next line so we does not need to write \n