# RULES & IMPORTANT NOTES

1.  String is a character array terminated by a '\0' (Null Character : it denotes string termination) 

2.   SCANF()  function  cannot input MULTI-Word STrings with spaces , so we use gets() -> fgets() for input and puts() for output